
Imports RunLibrary

Module Program

    Sub Main()
        'Menu.DrawAll()
        Dim txt As TextBox = New TextBox(20, 20, 10, 1)
        'Dim r As Run = New Run()
        'r.SelectApp = Run.Apps.Settings
        Console.ReadKey()
    End Sub

End Module
